# Introducción a los problemas de Machine Learning

## Tipos de problemas
1. Según el tipo de aprendizaje
    - **Supervisado**: hay un conjunto de datos etiquetados. Se utiliza para los problemas de clasificación.
    - **No supervisado**: no hay un conjunto de datos etiquetados, y se requiere resolver problemas de:
        + agrupamiento / clasificación (clustering)
        + topic modeling
    - **Semi supervisado**: hay un conjunto de datos etiquetados y no etiquetados
    - **Por refuerzo**: hay un agente que aprende a tomar decisiones (reinforcement learning)
2. Según tipo de predicción (algunos)
    - **Regresión**: se predice un número (continuo)
    - **Clasificación**: se predice una etiqueta, categoría, clase
        + Clasificación binaria
        + Clasificación multiclase ( + de 2 clases)
    - **Recomendación**: se predice una lista de elementos recomendados
    - **Anomalías**: se detectan anomalías en los datos
    - **Proyección (forecasting)**: se predice un valor futuro en una serie temporal
    - **Procesamiento de lenguaje natural**: se predice un valor en un texto

Ejemplos de problemas de acuerdo a su tipo de dato y tipo de aprendizaje

Nombre | Tipo de dato | Tipo de aprendizaje
--- | --- | ---
Regresión | Tabular | Supervisado
Clasificación | Tabular | Supervisado
Forecasting | Numérico | Supervisado | Proyección
Clasificación/segmentación de Imagen | Imagen | Supervisado
Detección de Objetos | Imagen | Supervisado
Clasificación de video | Video | Supervisado
Seguimiento de Objeto | Video | Supervisado
Análisis de Sentimiento | Texto | Supervisado
Traducción | Texto | Supervisado
Extracción de datos | Texto | Supervisado
Clustering | Tabular | No supervisado
Principal Component Analysis(PCA) | Tabular | No super  visado
Topic modeling | Texto | No supervisado

## Métricas

Distintos tipos de problemas requieren distintos tipos de métricas para evaluar el desempeño del modelo.
Evaluar el modelo implica verificar si el modelo entrenado es lo suficientemente preciso.

### Problema de clasificación binario
- Posibles resultados: 0 o 1 (verdadero o falso)
- Errores posibles
    + Falso positivo
    + Falso negativo

- Matriz de confusión
    + 
- Posible enfoques
    + Reducir los falsos positivos
    + Reducir los falsos negativos( por ejemplo clasificar si una persona tiene o no una enfermedad). La medida correcta es el recall.
    $$
    \text{Recall} = \frac{\text{Verdaderos positivos}}{\text{Verdaderos positivos} + \text{Falsos negativos}}
    $$
### Problema de clasificación multiclase
- Nos devuelve una distribución de probabilidades para cada clase
- Métricas
### Problema de regresión
- Objetivo es predecir un valor numérico
- Métricas
    + MAE (Mean Absolute Error). Nos muestra el promedio de la magnitud delos errores (errores positivos y negativos no se compensan)
    $$ 
    \text{MAE} = \frac{1}{n} \sum_{i=1}^{n} |y_i - \hat{y}_i| 
    $$
    + MSE (Mean Squared Error) Nos muestra el promedio de los cuadrados de los errores. Penaliza más los errores grandes.
    $$ 
    \text{MSE} = \frac{1}{n} \sum_{i=1}^{n} (y_i - \hat{y}_i)^2 
    $$
    + RMSE (Root Mean Squared Error) Es la raíz cuadrada del MSE. Nos muestra el promedio de los errores en la misma unidad que la variable objetivo.
    $$ 
    \text{RMSE} = \sqrt{\frac{1}{n} \sum_{i=1}^{n} (y_i - \hat{y}_i)^2} 
    $$
    + R^2 (Coefficient of Determination) Nos muestra la proporción de la varianza de la variable objetivo que es explicada por el modelo. Un valor de 1 indica que el modelo explica toda la varianza de la variable objetivo. Un valor de 0 indica que el modelo no explica nada de la varianza de la variable objetivo.
    $$ 
    \text{R}^2 = 1 - \frac{\sum_{i=1}^{n} (y_i - \hat{y}_i)^2}{\sum_{i=1}^{n} (y_i - \bar{y})^2} 
    $$




## Bibliografía
- Official Google Cloud certified Pro ML Engineer Handbook